name: Pull request
on:
  pull_request:

env:
  default_rootfs_version: "bullseye"

jobs:
  verify-and-scan:
    uses: ./.github/workflows/wf_verifyandscan.yaml
    with:
      rootfs_version: ${{ env.default_rootfs_version }}
